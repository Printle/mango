<div class="block">
  <h2>Opret todo</h2>
  <p>For <a href='<%= client_path(@conn, :show, @client) %>'><%= @client.name %></a></p>

  <%= render "form.html", changeset: @changeset,
                          action: todo_path(@conn, :create, client: @client.id) %>

  <span><%= link "Back", to: todo_path(@conn, :index) %></span>
</div>
